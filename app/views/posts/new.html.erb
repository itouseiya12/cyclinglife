<div class="cover-new">
  <div class="box-inner">
    <%= form_with model: @post, url:'/posts', local:true do |f| %>
    <%= render 'shared/error', object: f.object %>
    <h1>新規投稿</h1>
    <div class="form-group">
      <label class="form-label">撮影場所</label>
      <%= f.text_field :location,placeholder: "撮影した場所を入力", class: "form-control" %>
    </div>
    <div class="form-group">
      <label class="form-label">詳細</label>
      <%=f.text_area :text,placeholder: "撮影した場所の詳細を入力", class: "form-control" %>
    </div>
    <label class="form-image">
      <span class="image-file">クリックして投稿したい画像を選択</span>
      <%= f.file_field :postimage, id:"item-image", class:"hidden" %>
    </label>
    <div class="form-group">
      <label class="form-label">施設名、又は住所(任意)</label>
      <%= f.text_field :address,placeholder: "入力するとGoogleMap表示", class: "form-control" %>
    </div>
    <div class="post-button">
      <%= f.submit '投稿する', class: "btn btn-success" %>
    </div>
    <% end %>
  </div>
</div>
